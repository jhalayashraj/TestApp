<nav class="navbar navbar-default navbar-static-top navbar-inverse nav-primary">
  <div class="container-fluid">
    <!-- <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
    </div> -->

    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav nav-a">
        <!-- <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">TestApp </a>
          <ul class="dropdown-menu"> -->
            <li><%= link_to "<b>TestApp</b>".html_safe, posts_path, {:style=>'font-size: 18px;'} %></li>
          </ul>
        </li>
      </ul>

        <div class="navbar-right nav-flex user-box">
          <% if user_signed_in? %>
            <div class="dropdown">
              <span class="dropdown-toggle" id="dropdownMenu1" data-toggle="dropdown" aria-haspopup="true" aria-expanded="true" >
                <div class="circle-image profile-avatar">
                  <%= image_tag current_user.profile.avatar.url(:thumb), class: 'img-responsive' %>
                </div>
                  <span class="user-name">
                    <%= current_user.user_name.upcase %>
                  </span>
                  <span class="caret"></span>
              </span>
              <ul class="dropdown-menu" aria-labelledby="dropdownMenu1">
                <li><%= link_to "Edit Profile", edit_profile_path(current_user.profile) %></li>
                <li><%= link_to "Sign out", destroy_user_session_path, :method => 'delete' %></li>
              </ul>
            </div>
        
          <% else %>
            <%= link_to 'Sign Up', new_registration_path(resource_name), class: 'btn btn-primary' %>
          <% end %>
        </div>
      <!-- <ul class="nav navbar-nav navbar-right nav-flex">
        <%# if user_signed_in? %>
          <li>
            <div class="circle-image profile-avatar">
              <%#= image_tag current_user.profile.avatar.url(:thumb), class: 'img-responsive' %>
            </div>
          </li>
          <li><%#= link_to "Profile", edit_profile_path(current_user.profile) %></li>
          <li><%#= link_to "Sign out, #{current_user.email}", destroy_user_session_path, :method => 'delete' %></li>
        <%# else %>
          <li><%#= link_to 'Sign in', new_user_session_path %></li>
        <%# end %>
      </ul> -->
    </div>
  </div>
</nav>
<script>
  $('.dropdown-toggle').dropdown()
</script>