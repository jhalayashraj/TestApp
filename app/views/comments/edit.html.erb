<h1>Editing Comment</h1>

<%= render 'form', comment: @comment %>

<%#= link_to 'Show', post_comment_path(post_id: @comment.post_id, id: @comment.id) %>
<%= link_to 'Back', post_comment_path(post_id: @comment.post_id) %>
